[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm install", "python3 -m pip install opencv-python-headless numpy"]

[start]
cmd = "node server.js"
